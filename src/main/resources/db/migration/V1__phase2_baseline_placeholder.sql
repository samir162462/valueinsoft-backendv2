-- Placeholder baseline migration for the new Flyway structure.
-- The backend currently has legacy shared and tenant-managed schema creation.
-- Curated forward migrations should be added here incrementally as touched areas are stabilized.

SELECT 1 WHERE 1 = 0;
